import { createStore, } from 'vuex';
import { contacts } from "@/store/modules/contacts";

const modules = {
  contacts
};
const state = {};
const getters = {};
const mutations = {};
const actions = {};

export default createStore({
  modules,
  state,
  getters,
  mutations,
  actions
});
